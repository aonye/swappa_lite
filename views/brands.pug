extends layout

block content

    h1=title

    ul(class='brands-list')
      each brand in brand_list
        li
          a(href=`/brand/${brand._id}`) #{brand.name}
      else
        li There are no brands.